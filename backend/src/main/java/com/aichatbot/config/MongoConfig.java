package com.aichatbot.config;

/**
 * Mongo configuration removed. This placeholder remains to avoid classpath issues
 * if referenced elsewhere, but it is not a Spring configuration and contains no code.
 */
public class MongoConfig {
    // No-op
}
